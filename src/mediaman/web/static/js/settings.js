// Settings JS has been split into settings/*.js — this file is kept empty so
// any cached <script src="/static/js/settings.js"> from a previously-rendered
// template doesn't 404 during deployment. Remove once deployment is past the
// cutover.
